# CI/CD Pipeline is currently disabled.
# Remove this comment and restore the original content to re-enable.

name: CI/CD Pipeline (Disabled)
on:
  workflow_dispatch:

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Workflow is disabled"' 